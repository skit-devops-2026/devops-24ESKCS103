# CI Notes

The repository uses GitHub Actions for continuous integration.

The CI workflow is stored under:

`.github/workflows/ci.yml`

The workflow runs automatically when changes are pushed to the repository.
